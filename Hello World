/*
println : method
Hello : string in ""
out : object has print method
system : class has object(out) which has method(print)

jvm need a main() method, so we need main 
Main() will ask for string array and for returning we need void

method are of two type :
                          static : class
                          non static : object
                          
FileName : "HelloWorld.java". */
class HelloWorld 
{  
	public static void main(String args[]) 
	{ 
		System.out.println("Hello, World"); 
	} 
} 
